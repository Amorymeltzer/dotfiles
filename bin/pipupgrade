#!/usr/bin/env bash
# pipupgrade by Amory Meltzer
# Upgrade all pip modules
# http://stackoverflow.com/a/3452888/2521092

sudo -H pip list -o | grep -v '^\-e' | grep -v pip | grep -v gitdb2 | cut -d ' ' -f 1 | tail +3 | xargs sudo -H pip install -U
