#!/usr/bin/env bash
# pipupgrade by Amory Meltzer
# Upgrade all pip modules
# http://stackoverflow.com/a/3452888/2521092

sudo -H pip list --format=legacy -o | grep -v '^\-e' | grep -v bonjour-py | grep -v pip | cut -d ' ' -f 1 | xargs sudo -H pip install -U
